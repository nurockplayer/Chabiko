services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - venv:/app/.venv
    working_dir: /app

volumes:
  node_modules:
  venv:
